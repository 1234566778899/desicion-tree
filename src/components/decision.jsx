// src/components/Decision.js

import React from 'react';

const Decision = ({ result }) => {
  return (
    <div>
      <h2>Metodología recomendada: {result}</h2>
    </div>
  );
};

export default Decision;
